# AI Roundtable 技术设计

## 目标与边界

首版优先提供无需 API、无需网络的完整圆桌闭环。网页平台功能明确标为实验性：只使用用户自行登录的官方网页，不保存密码，不绕过验证码或平台限制；严格自动模式失败时重试并跳过，不要求用户复制粘贴。

## 分层

- `app/models`：Pydantic 结构化模型，是 UI、编排、存储和导出的共同数据契约。
- `app/providers`：统一 `AIProvider` 接口。模拟、人工、GPT、Kimi、元宝、豆包和 DeepSeek 适配器互相隔离；网页适配器共享安全基类但使用独立选择器和 Edge 配置。
- `app/orchestration`：基于 `asyncio` 的批量五阶段编排、并发、交互重试、超时、取消、瞬时进度和错误隔离。
- `app/storage`：SQLite 持久化完整讨论快照、事件、配置及人工回答。
- `app/services`：Markdown/JSON 导出、敏感信息检查和本地数据清理。
- `app/ui`：三栏 PySide6 群聊界面、动态五平台登录向导、图片头像、聊天气泡、阶段进度、协同增益面板和八维雷达图；交互设置即时持久化，在线/离线参与者在点击时互斥，讨论运行期间锁定本轮配置；后台事件循环避免阻塞 UI。

## 数据流

问题与约束 → 5 份差异化独立回答 → 5 次融合式匿名批量互评 → 5 份协同修订 → 本轮无利益冲突评审团评分 → 下一轮或主持综合 → SQLite 快照与 Markdown/JSON 导出。裁判同时参赛时使用并行留一法评分，标准五 AI 单轮约 21 次网页交互，四 AI 单轮约 17 次；若裁判是不参赛的外部评审者，则仍分别约为 17 次和 14 次。

独立阶段按参与顺序分配互补视角。评审结果新增独有贡献、跨方案组合建议和冲突裁决测试；修订者同时接收针对自己的评审、其他匿名方案的语义投影及融合简报，并输出借鉴项、已解决冲突和协同增益。主持阶段只接收每个参与者的最新语义方案以及去重后的融合材料，不接收网页原文和历史模型元数据。轮数限制为 1–3，以降低重复讨论和上下文膨胀。

裁判采用比较优先而非独立打分：先要求唯一排名、最佳项和淘汰项，再按固定锚点评八个维度。若指定裁判也是候选作者，编排器自动启用留一法评审团：所有成功参与者并行评分，但每人的输入中删除其自有方案，解析器再按允许别名白名单拒绝自评分和重复票，因此正常情况下每个候选获得相同数量的 `N-1` 份非作者评分。若某位评审者失败或漏项，编排器按所有候选的共同最小票数确定性裁剪多余票，并写入 `judge_panel_balanced` 事件，避免票数不等造成结构性优势。决策分权重为正确性 22%、约束匹配 16%、可执行性 16%、风险控制 14%、证据 12%、逻辑 10%、客观性 5%、不确定性表达 5%；正确性、约束匹配或风险控制任一低于 4 时触发淘汰门槛。解析器可从多余花括号等畸形批量 JSON 中逐项恢复评分；无法恢复的候选保持缺失并触发无冲突补评/低可信度，不生成统一中性分。

首轮评分同时包含独立答案和第 1 轮修订，以同一次裁判调用建立基线；后续轮次保存新的评分快照。编排层计算每个 AI 的决策分增量、八维增量、提升/退步维度和全轮平均效果。主持人默认选择聚合排名第一的主方案，偏离第一名必须提供至少两条可核验的新证据；本地降级也只选择第一名完整方案，不再拼接所有答案。

单个适配器错误会转换为结构化 `ErrorRecord`；其他适配器继续运行。少于两份有效独立回答时编排立即失败，避免把单个 AI 的输出伪装成圆桌结果。

## 网页适配策略

五个网页适配器读取各自的选择器 JSON，使用 `channel="msedge"` 和项目内独立持久化目录，不接管主 Edge 配置。回答读取优先使用平台语义选择器，并用发送前后的页面文本差异作后备。自动模式属于实验性能力；失败、验证码或页面变化时重试并跳过，显式人工备用模式仍保留。

网页回答仍以结构化字段供编排层解析，UI 通过 `format_ai_content` 转换为自然语言段落；不完整的流式 JSON 只显示生成状态。独立回答使用 `asyncio.gather` 等待全部平台调用结束，网页适配器同时要求停止按钮消失并达到稳定观察窗口，之后发出 `stage_barrier` 才允许进入互评。

在线与离线参与者在 UI 层强制互斥，避免模拟结果混入真实网页测试。“四 AI 测试（无 GPT）”预设只选择 Kimi、元宝、豆包和 DeepSeek，并设置 DeepSeek 主持、Kimi 裁判。网页适配器把发送前可见的错误文字记录为基线，只响应本次发送后新增的显式错误标记，避免历史会话中的“生成失败/服务繁忙”造成瞬时误判；无新回答 90 秒即失败，外层硬超时上限为 180 秒，所有超时日志均使用可读错误文本。

主持综合使用健康感知降级：若指定在线主持人未产生有效方案，则不再重复调用该失败平台，而是优先选择未担任裁判且响应更快的成功在线参与者；若主持人在综合调用中失败，也只尝试一次同样的在线接力。角色变化通过 `moderator_fallback` 事件、最终提供者名称、风险项和下调后的可信度完整记录；在线接力仍失败才进入本地降级综合器。

诊断使用两层日志：`application.log` 为脱敏滚动主日志，`run_<discussion_id>.jsonl` 为逐轮结构化事件日志。后者明确排除 `raw`、`prompt` 和瞬时回答正文，避免日志成为提示词或回答副本。

## 安全

不接收或保存密码/API Key；配置目录、数据库、日志和导出默认被 Git 忽略；日志过滤 Cookie、Token、Authorization 等常见认证字段；清除本地数据前要求 UI 二次确认。
